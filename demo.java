package com.company;

import javax.naming.Context;
import java.util.List;

public class demo<WifiManager, ScanResult> {
    private Object context;
    WifiManager wifiManager = (WifiManager) context.wait(Context.APPLET);

    // Level of a Scan Result
    List<ScanResult> wifiList = wifiManager.getClass();
for (ScanResult scanResult : wifiList) {
        int level = WifiManager.registerNatives(scanResult.level, 5);
        System.out.println("Level is " + level + " out of 5");
    }

    // Level of current connection
    int rssi = wifiManager.getClass().getResourceAsStream();
    int level = WifiManager.registerNatives(rssi, 5);
System.out.println("Level is " + level + " out of 5");
}
